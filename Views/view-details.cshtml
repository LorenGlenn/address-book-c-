<!DOCTYPE html>
<html>
  <head>
    <link rel="stylesheet" type="text/css" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
    <link rel="stylesheet" type="text/css" href="/Content/css/styles.css">
    <meta charset="utf-8">
    <title>Details</title>
  </head>
  <body>
    <div class="container">
      <h1>Information for @Model["contact"].GetName()</h1>
      <ul id="view-ul">
        <li>Name: @Model["contact"].GetName()
          <ul>
            <li>Phone Number: @Model["contact"].GetPhoneNumber()</li>
            <li>Details: @Model["contact"].GetDetails()</li>
            <li>Street: @Model["address"].GetStreet()</li>
            <li>City: @Model["address"].GetCity()</li>
            <li>State: @Model["address"].GetState()</li>
          </ul>
        </li>
      </ul>
      <div class="row">
        <div class="col-sm-3 col-sm-offset-3">
          <form action='/contacts/@Model["contact"].GetId()/remove' method="post">
            <button type="submit">Delete this contact</button>
          </form>
        </div>
        <div class="col-sm-3">
          <a class="button" href="/">Return to Address Book</a>
        </div>
      </div>
    </div>
  </body>
</html>

<!DOCTYPE html>
<html>
  <head>
    <link rel="stylesheet" type="text/css" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
    <link rel="stylesheet" type="text/css" href="/Content/css/styles.css">
    <meta charset="utf-8">
    <title>Contact Added</title>
  </head>
  <body>
    <h1>You've added a contact!</h1>
    <ul>
      <li>@Model["contact"].GetName()
        <ul>
          <li>@Model["contact"].GetPhoneNumber()</li>
          <li>@Model["contact"].GetDetails()</li>
          <li>@Model["address"].GetStreet()</li>
          <li>@Model["address"].GetCity()</li>
          <li>@Model["address"].GetState()</li>
        </ul>
      </li>
    </ul>

    <a href="/">Return to Address Book</a>
  </body>
</html>
